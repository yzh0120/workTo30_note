赋值运算符  = 



自加自减运算符 
	int a = 0
	a++
	++a   先 ++ 后和其他变量运算
	


关系运算符  结果都是boolean 类型
		 == 是否相等 
		 != 是否不相等
		 >  是否大于
		 >= 是否大于相等
		<   是否小于
		<=  是否小于相等
		
逻辑运算符 结果都是boolean 类型
		&  与
		|  或
		^  异或                 当a和b同真或同假时为假，不然为真。
		!  非
		
短路逻辑运算符 
		&&     (& 无论左边真假 右边表达式都会执行)  (&& 左边是真 右边才会执行)
		||     (| 无论左边真假 右边表达式都会执行)  (|| 左边是假 右边才会执行)
		
		
三元运算符
	a > b ? a:b;