Math所有的成员都是静态的 ,使用它不需要实例化

Math.abs() 返回参数的绝对值          入参什么类型 返回就是什么类型
Math.ceil() 将数字向上舍入到最接近的整数()             double类型
Math.floor()将数字向下舍入到最接近的整数()             double类型
Math.round() 按照四舍五入返回最接近参数的int
Math.max(a,b) 
Math.
Math.
Math.
Math.

     abs()                         返回参数的绝对值          入参什么类型 返回就是什么类型
double ceil(double a)              将数字向上舍入到最接近的整数()             double类型
double floor(double a)              将数字向下舍入到最接近的整数()             double类型
int round(float a)                  按照四舍五入返回最接近参数的int
int max(int a, int b)               返回两个值较大的int
int min(int a, int b)               返回两个值较小的int
double pow(double a,double b)       返回 a的b次方
double random()                    返回[0.0 - 1.0)